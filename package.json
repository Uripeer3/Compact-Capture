{
  "name": "compact-capture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "node benchmarks/documentRenderView.js",
    "check": "find src tests benchmarks -name '*.js' -exec node --check {} +",
    "test": "node --test"
  }
}
